package tests;

import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;

import models.Client2;
import models.Server2;

public class NetworkTest {

	@Before
	public void setUp() throws Exception {
		Server2 server = new Server2();
		Client2 client1 = new Client2();
		Client2 client2 = new Client2();
		Client2 client3 = new Client2();
		Thread a = new Thread((Runnable) server); 
		Thread b = new Thread(client1);
		Thread c = new Thread(client2);
		Thread d = new Thread(client3);
		a.start();
		b.start();
		c.start();
		d.start();
	}

	@Test
	public void test() {
		
	}

}
